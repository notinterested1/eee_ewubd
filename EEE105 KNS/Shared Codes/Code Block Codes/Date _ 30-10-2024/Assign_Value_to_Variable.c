/* Assign values to variables */
#include<stdio.h>

int main()
{
    float amount, inrate, value;
    char name;
    //int x, y, z;
    //x = y = z = 100;
    //float summation, total, capital;
    //capital = summation = total = 100.2;
    amount = 10000;
    inrate = 0.01;
    name = 's';

    value = amount + amount*inrate;

    printf("The value of y is: %d\n", y);
    printf("The name is: %f\n", total);
    printf("The name is: %c\n", name);
    printf("Total amount is: %f", value);

    return 0;
}
